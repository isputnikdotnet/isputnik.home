<?xml version="1.0"?>
<!--
  Unraid Community Applications template for isputnik.home — a DEPLOYMENT
  artifact (the app's install manifest), not source code. DO NOT DELETE.
  The <Icon> below is a raw.githubusercontent URL to Assets/brand/ on the main
  branch, so the repo-root Assets/ folder must also stay. (The web app uses its
  own copy under apps/web/public/Assets/; this template references the root one.)
-->
<Container version="2">
  <Name>isputnik</Name>
  <Repository>ghcr.io/isputnikdotnet/isputnik.home:latest</Repository>
  <Registry>https://ghcr.io/isputnikdotnet/isputnik.home</Registry>
  <Network>bridge</Network>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/isputnikdotnet/isputnik.home/issues</Support>
  <Project>https://github.com/isputnikdotnet/isputnik.home</Project>

  <Overview>
Private self-hosted family media library for audiobooks, ebooks, photos, and other digital media. Includes a modern web interface, installable PWA, offline listening support, user management, and library scanning. On first run, open the web UI to create an administrator account.
  </Overview>

  <Category>MediaApp:Books MediaServer:Books MediaApp:Other</Category>

  <WebUI>http://[IP]:[PORT:4000]/</WebUI>

  <TemplateURL>https://raw.githubusercontent.com/isputnikdotnet/isputnik.home/main/isputnik-home.xml</TemplateURL>

  <Icon>https://raw.githubusercontent.com/isputnikdotnet/isputnik.home/main/Assets/brand/isputnik-app-icon.png</Icon>

  <!-- Web Port -->

  <Config Name="Web UI Port"
          Target="4000"
          Default="4000"
          Mode="tcp"
          Description="Port used to access the iSputnik web interface."
          Type="Port"
          Display="always"
          Required="true"
          Mask="false">4000</Config>

  <!-- Application Data -->

  <Config Name="Config &amp; Data"
          Target="/config"
          Default="/mnt/user/appdata/isputnik"
          Mode="rw"
          Description="Stores database, metadata, thumbnails, backups, settings, and application data. Backups appear here under backups/."
          Type="Path"
          Display="always"
          Required="true"
          Mask="false">/mnt/user/appdata/isputnik</Config>

  <!-- Optional Media Path -->

  <Config Name="Media Storage"
          Target="/media"
          Default="/mnt/user/media"
          Mode="rw"
          Description="Media storage path. Must be Read/Write — uploading photos/videos and filing them into dated folders writes here (scanning alone only needs read). Additional storage locations can be added later by editing the container or from within iSputnik."
          Type="Path"
          Display="always"
          Required="false"
          Mask="false">/mnt/user/media</Config>

  <!-- Environment Variables -->

  <Config Name="PUID"
          Target="PUID"
          Default="99"
          Mode=""
          Description="User ID the app runs as. The server runs unprivileged (not root); on first start it takes ownership of the Config &amp; Data path as this user. 99 (nobody) matches the default owner of an Unraid appdata share — leave it unless your share is owned by a different user."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false">99</Config>

  <Config Name="PGID"
          Target="PGID"
          Default="100"
          Mode=""
          Description="Group ID the app runs as. 100 (users) matches the default group of an Unraid appdata share — leave it unless your share uses a different group."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false">100</Config>

  <Config Name="App URL"
          Target="APP_URL"
          Default=""
          Mode=""
          Description="The address the app is reached at (for example https://isputnik.example.com). Set this when you put a reverse proxy or tunnel in front: it fixes the browser origin the app accepts, and an https address is also what turns on HTTPS-only enforcement (HSTS). Leave blank for plain LAN access."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false"></Config>

  <Config Name="HSTS"
          Target="HSTS"
          Default=""
          Mode=""
          Description="Leave blank. When App URL starts with https:// the app tells browsers to stay on HTTPS for a year. Set to false to suppress that — for example when your reverse proxy already sends its own Strict-Transport-Security header. Nothing is sent for a plain-http App URL either way."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false"></Config>

  <Config Name="Cookie Secure"
          Target="COOKIE_SECURE"
          Default="auto"
          Mode=""
          Description="Leave on auto: secure cookies then follow App URL — on when it starts with https://, off for a plain http home install. Set true or false only to override that. Secure cookies are never sent over plain http, so forcing true on an http install drops the sign-in cookie and you cannot log in. (auto needs version 1.13.3 or newer.)"
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false">auto</Config>

  <Config Name="Session Length (days)"
          Target="SESSION_DAYS"
          Default="14"
          Mode=""
          Description="Number of days a login session remains valid."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false">14</Config>

  <Config Name="Invite Validity (days)"
          Target="INVITE_DAYS"
          Default="7"
          Mode=""
          Description="Number of days invitation links remain valid."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false">7</Config>

  <Config Name="Trusted Proxy Hops"
          Target="TRUST_PROXY_HOPS"
          Default=""
          Mode=""
          Description="Number of reverse proxies in front of the app (usually 1 behind a reverse proxy or Cloudflare Tunnel). Leave blank for direct LAN access. Needed for correct per-IP rate limiting, auto-block, and trusted networks — set it to match your proxy count, no higher."
          Type="Variable"
          Display="always"
          Required="false"
          Mask="false"></Config>

</Container>
node_modules/
dist/
.env
.env.local
.data
.claude

# Runtime data: SQLite DB, WAL/SHM, backups, uploaded media — never sync to remote
data/

# MFA secret-encryption key, persisted beside the DB when MFA_ENCRYPTION_KEY is unset
mfa.key

# Personal scratch notes under Documents — keep local only (case-insensitive)
Documents/notes/
Documents/Notes/
# Fetched at build time from a GitHub release (scripts/fetch-face-model.mjs),
# not carried in the repo — 167 MB through git-LFS is what broke image builds.
apps/server/models/face/w600k_r50.onnx

App shell — navigation, theme switching, protected routes
I like color sceme currently develop for index.html, escpecially backround, so we can keep this color scema for default theme.
Layout: I like the layout for applicaton audiobookshelf, where configuration menu for applicaton on the top right, and left menu small with icons. Also, username should be apper on the top of the applicaton with username and icon, when you click on the icon bring user profile page. 
Let's also add dark and light theme.
So, left menu will only contain Home and sign out and app version on the button.
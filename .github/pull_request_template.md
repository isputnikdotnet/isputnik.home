# What this changes

<!-- What the change does, in terms of what a user of the app would notice. -->

Fixes #

## Why

<!-- The problem being solved. Skip if the linked issue already covers it. -->

## How it was tested

<!-- What you actually ran or clicked through. "Scanned a 40-book library and
     checked the covers" beats "tested locally". -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run check:ui` passes (required for any UI change)
- [ ] `npm test` passes
- [ ] Schema changes: a new table edited directly into `schema.sql`, or a new
      column added with a migration — and `docs/database.md` updated
- [ ] User-facing changes: the relevant guide in `docs/users/` is updated
- [ ] For anything large, this was discussed in an issue first

<!-- By submitting, you agree your contribution is licensed under AGPL-3.0. -->
